#include "map.h"

/*
 * =================
 *  Map Definitions
 * =================
*/

Map::Map(MicroSD mSD, myFile file, int rows, int clms)
  : mSD(mSD), file(file), rows(rows), clms(clms) {} 








